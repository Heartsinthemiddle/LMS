package com.lms.entity;

/**
 * Enumeration representing user roles in the system.
 * ADMIN: Administrative privileges
 * CHILD: Standard user privileges
 */
public enum Role {
    ADMIN,
    PARENT,
    CHILD
}

